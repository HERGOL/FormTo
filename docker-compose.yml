
        services:
          formto:
            image: yidirk/formto:latest
            container_name: formto
            restart: unless-stopped
            ports:
              - "3080:3000"
            environment:
              - PORT=3080
              - RACK_ENV=production
              - ADMIN_PASSWORD=wMlM4w1S&CYjZ*q
            volumes:
              - ./data:/app/data